# Findings write-up

`seedlm-baseline.html` — the report as published (page content only; the
Artifact host supplies the document skeleton).

`seedlm-baseline-standalone.html` — the same page wrapped in a full HTML
document so it opens directly in a browser.

Published at https://claude.ai/code/artifact/2f84e1be-1c96-40a4-b721-f03fc750cc5f
